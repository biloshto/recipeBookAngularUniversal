import { enableProdMode } from '@angular/core';
export { AppServerModule } from './app/app.server.module';
// this is needed for server side rendering where we will reference the main.server file as a entry point


enableProdMode();